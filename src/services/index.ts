// Firebase
export { app, db, auth, storage, googleProvider } from './firebase';

// Firestore helpers
export * from './firestore';

// Legacy API (deprecated, use Firestore helpers instead)
export { api } from './api';
